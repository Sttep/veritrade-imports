# Datos de trabajo (xlsx) — ignorados por defecto para no publicar data privada.
*.xlsx
# Bronze y Silver: datos crudos y estructurados (privados)
data/bronze/*.xlsx
data/silver/*.xlsx
# Parquets Gold sí se versionan (servidos por Streamlit Cloud)
!data/gold/*.parquet

# Archivos de ejemplo nombrados explícitamente
!data/ejemplo.xlsx

__pycache__/
.cache/
.env
CLAUDE.md
.claude/

# Web service runtime (legacy)
inputs/
outputs/
jobs/
backend.db
frontend/node_modules/
frontend/dist/
frontend/.vite/
